<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>view form</title>
  </head>
  <body>
    <h1>Create Form</h1>

    <form action="/show" method="post">
      <input
        type="text"
        placeholder="Enter you select field"
        name="selectName"
      />

      <select name="type" id="type" onclick="addMore()">
        <option value="">Select your type of value</option>
        <option value="dropdown">Drop Down</option>
        <option value="checkbox">Check box</option>
        <option value="radio">Radio button</option>
      </select>

      <span onclick="addMoreSelect()">Add More Select</span>
      <br />
      <div id="selects"></div>
      <br />

      <br />
      <div id="fields"></div>
      <br />

      <input type="submit" onclick="submited()" />
    </form>
  </body>

  <script>
    let type = document.getElementById("type");
    let fields = document.getElementById("fields");

    let curr = "";

    let count = 2;
    function addMoreSelect() {
      fetch("/editselect", {
        method: "POST",
        headers: {
          "content-type": "application/json",
        },
      })
        .then((res) => {
          return res.json();
        })
        .then((res) => {
          let selects = document.getElementById("selects");

          let s = ` <input
        type="text"
        placeholder="Enter you select field"
        name="selectName"
      />

      <select name="type" id="type" onclick="addMore()">
        <option value="">Select your type of value</option>
        <option value="dropdown">Drop Down</option>
        <option value="checkbox">Check box</option>
        <option value="radio">Radio button</option>
      </select>

      <span onclick="addMoreSelect()">Add More Select</span>`;
        });
    }

    function addMore() {
      fetch("/edit", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
      })
        .then((res) => {
          return res.json();
        })
        .then((res) => {
          let texts = document.querySelectorAll("input[type=text]");

          console.log(texts);

          let span = `<span>addMore</span>`;

          const node = document.createElement("input");
          const node2 = document.createElement("span");

          node2.textContent = "add More";
          node.setAttribute("type", "text");
          node2.setAttribute("onclick", "addMore()");
          node.setAttribute("name", "optionfield");
          fields.appendChild(node);
          fields.appendChild(node2);

          // fields.innerHTML = curr;
        });
    }
  </script>
</html>
